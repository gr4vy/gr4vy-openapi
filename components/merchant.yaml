---
title: Merchant
type: object
description: A merchant in a cluster

x-tags:
  - Merchants

properties:
  type:
    type: string
    description: "`merchant`"
    example: merchant
    enum:
      - merchant

  short_name:
    type: string
    description: |-
      The unique short-name of the merchant, as used in their Gr4vy URLs. For
      example, a short-name of `nike` would result in the Gr4vy cluster's API
      being hosted on `https://api.nike.gr4vy.com`.
    example: acme

  display_name:
    type: string
    description: |-
      The name of this merchant as it will be displayed in the admin UI.
    example: Example Inc
