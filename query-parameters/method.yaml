---
schema:
  type: string
  example: card
  enum:
    - card
    - paypal
in: query
name: method
description: |-
  Filters the results to only the items for which the `method` has been set to
  this value.
