---
title: Card
description: A registered card
type: object
x-tags:
  - Payment Methods

allOf:
  - properties:
      method:
        type: string
        description: "`card`"
        example: card
        enum:
          - card

      payment_method_status:
        type: string
        description: |-
          The state of the card tokenization.
        example: registered
        enum:
          - registered

      details:
        $ref: |-
          ../openapi.yaml#/components/schemas/CardDetails
  - $ref: "../openapi.yaml#/components/schemas/PaymentMethod"
