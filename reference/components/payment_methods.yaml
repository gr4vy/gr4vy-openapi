---
title: Cards
type: object
description: A list of stored payment methods

x-tags:
  - PaymentMethods

properties:
  items:
    type: array
    description: A list of stored payment methods
    items:
      oneOf:
        - $ref: "../openapi.yaml#/components/schemas/Card"
        - $ref: "../openapi.yaml#/components/schemas/PayPal"

  limit:
    type: integer
    description: "The limit set to the number of items returned. "
    format: int32
    minimum: 0
    maximum: 1000
    example: 100
    default: 100

  next_page:
    type: string
    description: The marker that represents the next page of results.
    minLength: 1
    maxLength: 1000
    example: ZXhhbXBsZTE=
    nullable: true

  previous_page:
    type: string
    minLength: 1
    maxLength: 1000
    description: The marker that represents the previous page of results.
    example: ZXhhbXBsZTI=
    nullable: true
