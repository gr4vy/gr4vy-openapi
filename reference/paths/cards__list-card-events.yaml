---
operationId: list-card-event
summary: List card events
description: Returns a list of events for a given transaction.
tags: ["Cards"]

responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: "../openapi.yaml#/components/schemas/CardEvents"

  "401":
    description: Returns an error if no valid authentication was provided.
    content:
      application/json:
        schema:
          $ref: "../openapi.yaml#/components/schemas/Error401"
