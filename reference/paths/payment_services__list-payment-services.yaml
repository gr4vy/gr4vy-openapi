---
operationId: list-payment-services
summary: List payment services
description:
  Lists the currently configured and activated payment services for a for a
  merchant.
tags: ["Payment Services"]

responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: "../openapi.yaml#/components/schemas/PaymentServices"

  "401":
    description: Returns an error if no valid authentication was provided.
    content:
      application/json:
        schema:
          $ref: "../openapi.yaml#/components/schemas/Error401Unauthorized"
