---
schema:
  type: string
  example: USD
in: query
name: currency
description: |-
  Filters the results to only the items that match for this currency code. A
  currency is formatted as 3-letter ISO currency code.
