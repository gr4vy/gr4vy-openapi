---
schema:
  type: boolean
  example: true
  default: true
in: query
name: live_environment
description: |-
  Filters the results to only the items available in the live environment or if
  it should only show the items available in the sandbox/test environment. This
  value defaults to `true` if not provided.
