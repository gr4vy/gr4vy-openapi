---
# yamllint disable rule:line-length
title: Card Ruleset (Update)
type: object
description: |-
  Updates a card ruleset which holds the rules that are parsed for a card
  transaction.
x-internal: true
x-tags:
  - Card Rulesets

allOf:
  - $ref: "../openapi.yaml#/components/schemas/CardRulesetCreateRequest"
