---
title: Billing details request
type: object
description: |-
  Billing details to use associated to a buyer.

x-tags:
  - Buyers

required:
  - line1
  - city
  - state
  - postal_code
  - country

properties:
  city:
    type: string
    minLength: 1
    maxLength: 100
    description: City to use for this request.
    example: London
    nullable: true

  country:
    type: string
    minLength: 1
    maxLength: 2
    description: Country to use for this request.
    example: "GB"
    nullable: true

  postal_code:
    type: string
    minLength: 1
    maxLength: 50
    description: Postal code to use for this request.
    example: "789123"
    nullable: true

  state:
    type: string
    minLength: 1
    maxLength: 255
    description: The City to use for this request.
    example: "Greater London"
    nullable: true

  house_number_or_name:
    type: string
    minLength: 1
    maxLength: 255
    description: The house number or name to use for this request.
    example: "10"
    nullable: true

  line1:
    type: string
    minLength: 1
    maxLength: 255
    description: The first line of the address to use for this request.
    example: "10 Oxford Street"
    nullable: true

  line2:
    type: string
    minLength: 1
    maxLength: 255
    description: The second line of the address to use for this request.
    example: "New Oxford Court"
    nullable: true

  organization:
    type: string
    minLength: 1
    maxLength: 255
    description: The second line of the address to use for this request.
    example: "Gr4vy"
    nullable: true
