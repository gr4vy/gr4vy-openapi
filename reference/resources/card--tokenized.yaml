---
title: Card (Tokenized)
description: A mini format version of the card.
type: object
x-tags:
  - Payment Methods

properties:
  type:
    type: string
    description: "`payment-method`."
    example: payment-method
    enum:
      - payment-method

  id:
    type: string
    description: |-
      The unique ID of the payment method.
    format: uuid
    example: 77a76f7e-d2de-4bbc-ada9-d6a0015e6bd5

  method:
    type: string
    description: "`card`."
    example: card
    enum:
      - card

  details:
    $ref: |-
      ../openapi.yaml#/components/schemas/CardDetails
