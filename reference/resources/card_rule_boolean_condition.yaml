---
title: Card rule (Boolean condition)
type: object
description: |-
  Part of a rule that matches boolean fields. It defines the condition under
  which this rule applies.
x-internal: true
x-tags:
  - Card Rules

required:
  - match
  - key
  - value

properties:
  match:
    type: string
    description: "`boolean`."
    example: boolean
    enum:
      - boolean

  key:
    type: string
    description: The transaction field to filter by.
    example: store
    enum:
      - store
      - use_test_environment

  value:
    type: boolean
    description: |-
      The values to compare the `key` to.
    example: true
